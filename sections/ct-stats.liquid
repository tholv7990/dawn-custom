{% render 'ct-section-padding', section: section %}

<section class="gs-scope ct-stats section-{{ section.id }}-padding">
  <div class="gs-wrap">
    {%- unless section.settings.heading == blank -%}
      <h2 class="gs-section-heading">{{ section.settings.heading | escape }}</h2>
    {%- endunless -%}
    {%- unless section.settings.subheading == blank -%}
      <p class="gs-section-sub">{{ section.settings.subheading | escape }}</p>
    {%- endunless -%}

    {%- if section.blocks.size > 0 -%}
      <ul class="ct-stats__grid" role="list">
        {%- for block in section.blocks -%}
          <li class="ct-stats__item" {{ block.shopify_attributes }}>
            <p class="ct-stats__value">
              <span class="ct-stats__number">{{ block.settings.value | escape }}</span>
              {%- unless block.settings.suffix == blank -%}
                <span class="ct-stats__suffix">{{ block.settings.suffix | escape }}</span>
              {%- endunless -%}
            </p>
            <p class="ct-stats__label">{{ block.settings.label | escape }}</p>
          </li>
        {%- endfor -%}
      </ul>
    {%- endif -%}
  </div>
</section>

{% style %}
  .ct-stats {
    background: var(--ct-bg);
    color: var(--ct-text);
  }

  .ct-stats .gs-section-heading {
    text-align: center;
  }

  .ct-stats .gs-section-sub {
    max-width: 56ch;
    margin-inline: auto;
    margin-bottom: var(--ct-space-5);
    text-align: center;
    color: var(--ct-text-2);
  }

  .ct-stats__grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: var(--ct-space-4);
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .ct-stats__item {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: var(--ct-space-2);
    text-align: center;
    padding: var(--ct-space-6) var(--ct-space-4);
    background: var(--ct-bg-surface);
    border: 1px solid var(--ct-border);
    border-radius: var(--ct-r-lg);
    transition: border-color 0.2s ease, transform 0.2s ease;
  }

  .ct-stats__item:hover {
    border-color: var(--ct-action-border);
    transform: translateY(-2px);
  }

  .ct-stats__value {
    display: flex;
    align-items: baseline;
    justify-content: center;
    gap: var(--ct-space-1);
    margin: 0;
    color: var(--ct-action);
    font-size: var(--ct-h2);
    font-weight: var(--ct-fw-bold);
    line-height: var(--ct-leading-tight);
    letter-spacing: -0.01em;
  }

  .ct-stats__suffix {
    font-size: var(--ct-h3);
    font-weight: var(--ct-fw-bold);
    line-height: var(--ct-leading-tight);
    color: var(--ct-action);
  }

  .ct-stats__label {
    margin: 0;
    color: var(--ct-text-2);
    font-size: var(--ct-text-base);
    font-weight: var(--ct-fw-medium);
    line-height: var(--ct-leading-base);
  }

  @media screen and (min-width: 750px) {
    .ct-stats__grid {
      grid-template-columns: repeat(4, minmax(0, 1fr));
      gap: var(--ct-space-5);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .ct-stats__item {
      transition: none;
    }

    .ct-stats__item:hover {
      transform: none;
    }
  }
{% endstyle %}

{% schema %}
{
  "name": "Stats band",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "By the numbers"
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "Subheading",
      "info": "Use real, verifiable figures only. Edit each stat below with your own numbers and labels.",
      "default": "Add your own real metrics here — replace each placeholder with a number you can stand behind."
    },
    {
      "type": "header",
      "content": "Section padding"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "label": "Top padding",
      "default": 60
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "label": "Bottom padding",
      "default": 60
    }
  ],
  "blocks": [
    {
      "type": "stat",
      "name": "Stat",
      "settings": [
        {
          "type": "text",
          "id": "value",
          "label": "Value",
          "info": "Enter a real number. Leave at 0 until you have a figure you can verify.",
          "default": "0"
        },
        {
          "type": "text",
          "id": "suffix",
          "label": "Suffix",
          "info": "Optional unit or symbol, e.g. %, +, hrs, ★. Leave blank if not needed.",
          "default": ""
        },
        {
          "type": "text",
          "id": "label",
          "label": "Label",
          "default": "Add a stat"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Stats band",
      "blocks": [
        { "type": "stat" },
        { "type": "stat" },
        { "type": "stat" },
        { "type": "stat" }
      ]
    }
  ]
}
{% endschema %}
