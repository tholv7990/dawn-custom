{{ 'ct-sections.css' | asset_url | stylesheet_tag }}
{{ 'section-ct-email.css' | asset_url | stylesheet_tag }}

{% render 'ct-section-padding', section: section %}

{%- assign section_id = section.settings.anchor_id | default: section.id -%}

<section id="{{ section_id }}" class="ct-email section-{{ section.id }}-padding color-{{ section.settings.color_scheme }}">
  <div class="ct-email-inner" data-reveal>
    {%- if section.settings.kicker != blank -%}<div class="ct-kicker">{{ section.settings.kicker }}</div>{%- endif -%}
    {%- if section.settings.heading != blank -%}<h2 class="ct-email-title">{{ section.settings.heading }}</h2>{%- endif -%}
    {%- if section.settings.subheading != blank -%}<p class="ct-email-sub">{{ section.settings.subheading }}</p>{%- endif -%}

    {%- form 'customer', class: 'ct-email-formwrap', data-ct-newsletter-form: '' -%}
      <input type="hidden" name="contact[tags]" value="newsletter">
      <input type="hidden" name="contact[accepts_marketing]" value="true">
      {%- if form.posted_successfully? -%}
        <span hidden data-ct-newsletter-success></span>
        <div class="ct-email-success">
          <div class="ct-email-success__icon">✓</div>
          <div class="ct-email-success__title">{{ section.settings.success_title }}</div>
          <div class="ct-email-success__text">{{ section.settings.success_text }}</div>
        </div>
      {%- else -%}
        <div class="ct-email-form">
          <input
            class="ct-email-input"
            type="email"
            name="contact[email]"
            placeholder="{{ section.settings.placeholder }}"
            aria-label="{{ section.settings.placeholder }}"
            autocorrect="off"
            autocapitalize="off"
            autocomplete="email"
            required
          >
          <button type="submit" class="ct-btn ct-btn--primary" disabled aria-disabled="true">{{ section.settings.button_label }}</button>
        </div>
        <span class="ct-email-error" data-newsletter-err>Please enter a valid email address.</span>
        {%- if form.errors -%}
          <div class="ct-email-error">{{ form.errors.translated_fields.email | capitalize }} {{ form.errors.messages.email }}</div>
        {%- endif -%}
      {%- endif -%}
    {%- endform -%}

    {%- if section.settings.disclaimer != blank -%}<div class="ct-email-fine">{{ section.settings.disclaimer }}</div>{%- endif -%}
  </div>
</section>

{% schema %}
{
  "name": "CT Email",
  "tag": "section",
  "class": "section",
  "disabled_on": { "groups": ["header", "footer"] },
  "settings": [
    { "type": "text", "id": "anchor_id", "label": "Anchor ID", "default": "signup" },
    { "type": "text", "id": "kicker", "label": "Kicker", "default": "Join the litter" },
    { "type": "text", "id": "heading", "label": "Heading", "default": "15% off your first order — any size" },
    { "type": "textarea", "id": "subheading", "label": "Subheading", "default": "Join the list and we'll email your 15% code, plus first dibs on new colors. No spam, unsubscribe anytime." },
    { "type": "text", "id": "placeholder", "label": "Input placeholder", "default": "you@email.com" },
    { "type": "text", "id": "button_label", "label": "Button label", "default": "Send my code" },
    { "type": "text", "id": "disclaimer", "label": "Fine print", "default": "We'll email your code · 15% off your first order, any value · one discount per order — offers don't combine" },
    { "type": "header", "content": "Success message" },
    { "type": "text", "id": "success_title", "label": "Success title", "default": "You're in!" },
    { "type": "text", "id": "success_text", "label": "Success text", "default": "Check your inbox for a welcome email." },
    { "type": "header", "content": "Section" },
    { "type": "color_scheme", "id": "color_scheme", "label": "Color scheme", "default": "scheme-1" },
    { "type": "range", "id": "padding_top", "label": "Top padding", "min": 0, "max": 100, "step": 4, "unit": "px", "default": 44 },
    { "type": "range", "id": "padding_bottom", "label": "Bottom padding", "min": 0, "max": 100, "step": 4, "unit": "px", "default": 44 }
  ],
  "presets": [ { "name": "CT Email" } ]
}
{% endschema %}
